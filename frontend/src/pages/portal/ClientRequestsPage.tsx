import React from "react";

export default function ClientRequestsPage() {
    return (
        <div className="p-4">
            <h1 className="text-xl font-semibold">My Sample Requests</h1>
            <p className="text-sm opacity-80 mt-1">
                Placeholder (Step 2). Step 3 will implement list + create draft + submit.
            </p>
        </div>
    );
}
